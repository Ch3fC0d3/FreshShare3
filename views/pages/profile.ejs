<style>
    /* Variables for consistent styling */
    :root {
        --primary-color: #63b175;
        --secondary-color: #4a8f5c;
        --text-color: #333;
        --light-bg: #f5f5f5;
        --border-color: #ddd;
    }

    /* Layout containers */
    .profile-container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 var(--spacing-md);
        margin-top: 120px; /* Add margin to account for fixed header */
    }

    /* Profile header section */
    .profile-header {
        background: white;
        border-radius: var(--border-radius);
        padding: var(--spacing-xl);
        margin-bottom: var(--spacing-xl);
        box-shadow: var(--card-shadow);
    }

    .profile-info {
        display: grid;
        grid-template-columns: auto 1fr auto;
        gap: var(--spacing-xl);
        align-items: center;
    }

    .profile-avatar {
        width: 150px;
        height: 150px;
        border-radius: 50%;
        overflow: hidden;
        background: var(--secondary);
        box-shadow: var(--card-shadow);
    }

    .profile-avatar img {
        width: 100%;
        height: 100%;
        object-fit: cover;
    }

    .profile-details h1 {
        margin: 0 0 var(--spacing-sm) 0;
        color: var(--text-dark);
        font-size: 2rem;
    }

    .profile-details p {
        margin: 0;
        color: var(--text-light);
    }

    .profile-actions {
        margin-top: var(--spacing-md);
        display: flex;
        gap: var(--spacing-md);
    }

    /* Statistics grid */
    .stats-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
        gap: var(--spacing-lg);
        margin: var(--spacing-xl) 0;
    }

    .stat-card {
        background: white;
        padding: var(--spacing-lg);
        border-radius: var(--border-radius);
        box-shadow: var(--card-shadow);
        transition: transform 0.2s ease-in-out;
    }

    .stat-card:hover {
        transform: translateY(-5px);
    }

    .stat-label {
        color: var(--text-light);
        font-size: 0.9rem;
        margin-bottom: var(--spacing-sm);
    }

    .stat-value {
        font-size: 2.5rem;
        font-weight: 600;
        color: var(--text-dark);
        margin-bottom: var(--spacing-md);
    }

    .stat-footer {
        font-size: 0.9rem;
        display: flex;
        align-items: center;
    }

    .trend-up {
        color: var(--primary);
        display: flex;
        align-items: center;
    }

    .trend-down {
        color: #F44336;
        display: flex;
        align-items: center;
    }

    .trend-icon {
        margin-right: var(--spacing-sm);
    }

    /* Charts section */
    .charts-section {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(500px, 1fr));
        gap: var(--spacing-xl);
        margin: var(--spacing-xl) 0;
    }

    .chart-card {
        background: white;
        border-radius: var(--border-radius);
        padding: var(--spacing-lg);
        box-shadow: var(--card-shadow);
    }

    .chart-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: var(--spacing-lg);
    }

    .chart-header h2 {
        margin: 0;
        font-size: 1.25rem;
        color: var(--text-dark);
    }

    .chart-actions {
        display: flex;
        gap: var(--spacing-sm);
    }

    .chart-btn {
        background: var(--secondary);
        border: none;
        padding: var(--spacing-sm) var(--spacing-md);
        border-radius: 8px;
        color: var(--text-dark);
        cursor: pointer;
        transition: all 0.2s ease;
        font-weight: 500;
    }

    .chart-btn:hover,
    .chart-btn.active {
        background: var(--primary);
        color: white;
    }

    .chart-container {
        height: 300px;
        position: relative;
    }

    /* Profile sections */
    .profile-sections {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
        gap: var(--spacing-xl);
        margin-top: var(--spacing-xl);
    }

    .profile-section {
        background: white;
        border-radius: var(--border-radius);
        padding: var(--spacing-lg);
        box-shadow: var(--card-shadow);
    }

    .section-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: var(--spacing-lg);
    }

    .section-header h2 {
        margin: 0;
        font-size: 1.25rem;
        color: var(--text-dark);
    }

    .edit-btn {
        background: none;
        border: none;
        color: var(--primary);
        cursor: pointer;
        padding: 0;
        font-weight: 500;
        transition: color 0.2s ease;
    }

    .edit-btn:hover {
        color: var(--primary-dark);
        text-decoration: underline;
    }

    /* Activity list */
    .activity-list {
        list-style: none;
        padding: 0;
        margin: 0;
    }

    .activity-item {
        display: flex;
        align-items: flex-start;
        gap: var(--spacing-md);
        padding: var(--spacing-md) 0;
        border-bottom: 1px solid var(--border);
        transition: background-color 0.2s ease;
    }

    .activity-item:last-child {
        border-bottom: none;
    }

    .activity-item:hover {
        background-color: var(--secondary);
    }

    .activity-icon {
        width: 2.5rem;
        height: 2.5rem;
        border-radius: 50%;
        background: var(--primary-light);
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        flex-shrink: 0;
    }

    .activity-content {
        flex: 1;
    }

    .activity-title {
        margin: 0 0 var(--spacing-sm) 0;
        color: var(--text-dark);
        font-weight: 500;
    }

    .activity-time {
        font-size: 0.875rem;
        color: var(--text-light);
    }

    /* Responsive adjustments */
    @media (max-width: 768px) {
        .profile-info {
            grid-template-columns: 1fr;
            text-align: center;
        }

        .profile-avatar {
            margin: 0 auto;
        }

        .profile-actions {
            justify-content: center;
        }

        .charts-section {
            grid-template-columns: 1fr;
        }
    }
</style>

<div class="main-content">
    <div class="profile-container">
        <!-- Profile Header -->
        <div class="profile-header">
            <div class="profile-info">
                <div class="profile-avatar">
                    <img src="/images/default-avatar.png" alt="Profile Picture">
                </div>
                <div class="profile-details">
                    <h1><%= locals.user ? locals.user.username : 'User' %></h1>
                    <p>Member since <%= locals.user ? new Date(locals.user.createdAt).toLocaleDateString() : 'Unknown' %></p>
                    <div class="profile-actions">
                        <button class="btn btn-primary">Edit Profile</button>
                        <button class="btn btn-secondary">Change Password</button>
                    </div>
                </div>
            </div>
        </div>

        <!-- Stats Grid -->
        <div class="stats-grid">
            <div class="stat-card">
                <div class="stat-label">Total Items Shared</div>
                <div class="stat-value">42</div>
                <div class="stat-footer">
                    <span class="trend-up"><i class="fas fa-arrow-up trend-icon"></i>12%</span>
                    <span style="margin-left: 5px; color: var(--text-light);">from last month</span>
                </div>
            </div>
            
            <div class="stat-card">
                <div class="stat-label">People Helped</div>
                <div class="stat-value">68</div>
                <div class="stat-footer">
                    <span class="trend-up"><i class="fas fa-arrow-up trend-icon"></i>8%</span>
                    <span style="margin-left: 5px; color: var(--text-light);">from last month</span>
                </div>
            </div>
            
            <div class="stat-card">
                <div class="stat-label">Food Waste Reduced (kg)</div>
                <div class="stat-value">156</div>
                <div class="stat-footer">
                    <span class="trend-up"><i class="fas fa-arrow-up trend-icon"></i>15%</span>
                    <span style="margin-left: 5px; color: var(--text-light);">from last month</span>
                </div>
            </div>
            
            <div class="stat-card">
                <div class="stat-label">CO₂ Emissions Saved (kg)</div>
                <div class="stat-value">234</div>
                <div class="stat-footer">
                    <span class="trend-up"><i class="fas fa-arrow-up trend-icon"></i>9%</span>
                    <span style="margin-left: 5px; color: var(--text-light);">from last month</span>
                </div>
            </div>
        </div>

        <!-- Charts Section -->
        <div class="charts-section">
            <div class="chart-card">
                <div class="chart-header">
                    <h2>Impact Over Time</h2>
                    <div class="chart-actions">
                        <button class="chart-btn" id="weeklyBtn">Weekly</button>
                        <button class="chart-btn" id="monthlyBtn">Monthly</button>
                        <button class="chart-btn" id="yearlyBtn">Yearly</button>
                    </div>
                </div>
                <div class="chart-container">
                    <canvas id="impactChart"></canvas>
                </div>
            </div>
            
            <div class="chart-card">
                <div class="chart-header">
                    <h2>Food Category Breakdown</h2>
                </div>
                <div class="chart-container">
                    <canvas id="categoryChart"></canvas>
                </div>
            </div>
        </div>

        <div class="profile-sections">
            <div class="profile-section">
                <div class="section-header">
                    <h2>Personal Information</h2>
                    <button class="edit-btn">Edit</button>
                </div>
                <div class="section-content">
                    <div class="preferences-list">
                        <li>
                            <span>Email</span>
                            <span><%= locals.user ? locals.user.email : 'Not set' %></span>
                        </li>
                        <li>
                            <span>Phone</span>
                            <span>Not set</span>
                        </li>
                        <li>
                            <span>Location</span>
                            <span>Not set</span>
                        </li>
                    </div>
                </div>
            </div>

            <div class="profile-section">
                <div class="section-header">
                    <h2>Recent Activity</h2>
                    <a href="#" class="edit-btn">View All</a>
                </div>
                <div class="section-content">
                    <div class="activity-list">
                        <div class="activity-item">
                            <div class="activity-icon">
                                <i class="fas fa-share-alt"></i>
                            </div>
                            <div class="activity-content">
                                <p class="activity-title">Shared 2kg of vegetables</p>
                                <p class="activity-time">2 hours ago</p>
                            </div>
                        </div>
                        <div class="activity-item">
                            <div class="activity-icon">
                                <i class="fas fa-handshake"></i>
                            </div>
                            <div class="activity-content">
                                <p class="activity-title">Received bread from Downtown Bakery</p>
                                <p class="activity-time">Yesterday</p>
                            </div>
                        </div>
                        <div class="activity-item">
                            <div class="activity-icon">
                                <i class="fas fa-star"></i>
                            </div>
                            <div class="activity-content">
                                <p class="activity-title">Got a 5-star rating</p>
                                <p class="activity-time">3 days ago</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    // Initialize charts when the page loads
    document.addEventListener('DOMContentLoaded', function() {
        // Impact Over Time Chart
        const impactCtx = document.getElementById('impactChart').getContext('2d');
        new Chart(impactCtx, {
            type: 'line',
            data: {
                labels: ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun'],
                datasets: [{
                    label: 'Food Shared (kg)',
                    data: [65, 78, 90, 85, 95, 110],
                    borderColor: '#4CAF50',
                    tension: 0.4,
                    fill: false
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    legend: {
                        position: 'bottom'
                    }
                }
            }
        });

        // Food Category Breakdown Chart
        const categoryCtx = document.getElementById('categoryChart').getContext('2d');
        new Chart(categoryCtx, {
            type: 'doughnut',
            data: {
                labels: ['Vegetables', 'Fruits', 'Bakery', 'Dairy', 'Grains'],
                datasets: [{
                    data: [30, 25, 20, 15, 10],
                    backgroundColor: [
                        '#4CAF50',
                        '#8BC34A',
                        '#CDDC39',
                        '#FFEB3B',
                        '#FFC107'
                    ]
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    legend: {
                        position: 'bottom'
                    }
                }
            }
        });
    });
</script>
